name: Supply chain

'on':
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: '29 3 * * 1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    permissions:
      contents: read
    uses: Tinkora/.github/.github/workflows/reusable-supply-chain.yml@e967aed0860957b24daf57e66766713c60b5bcae # Tinkora organization baseline
    with:
      working-directory: .
      toolchain: 1.88.0
